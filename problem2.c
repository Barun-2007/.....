#include <stdio.h>
#include<math.h>
int main(){
    int n,a,k,l,sum=0;
    int arr;
    printf("Enter a number to check if it is armstrong number or not",n);
    scanf("%d",&n);
    k=sizeof(int)/sizeof(arr[0]);
    return 0; 
}